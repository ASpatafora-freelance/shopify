<h1>Questa è la sezione battiscopa</h1>
{% include "prod-data" %}
<ul>
{% for z in jsonprodata %}
  {% for i in z.class  %}
    <li>{{i.name}} ({{i.id}})</li>
  {% endfor %}
{% endfor %}
</ul>
<ul>
{% for i in jsonprodata.class.students  %}
    <li>{{i.name}} ({{i.iq}})</li>
{% endfor %}
</ul>
{% schema %}
  {
    "name": "battiscopa",
    "templates": ["index", "page", "product"],
  "presets": [
    {
      "name": "battiscopa",
      "category": "reference"
    }
  ]
  }
{% endschema %}
